#include <bits/stdc++.h>

using namespace std;

#define ll long long

void solve() {
  int k, x;
  cin >> k >> x;
  cout << k * x + 1 << "\n";
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(nullptr);
  int T;
  cin >> T;
  while (T--)
    solve();
}
