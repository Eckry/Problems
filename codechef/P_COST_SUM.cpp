#include <bits/stdc++.h>

using namespace std;

#define ll long long

#define optimize()                                                             \
  cin.tie(0);                                                                  \
  cin.sync_with_stdio(0);                                                      \
  ios_base::sync_with_stdio(0);                                                \
  cout.tie(0);

const int MOD  = 998244353;

void solve() {
  int n; cin >> n;
  vector<int> a(n);
  for(auto &c : a) cin >> c;
}

int main() {
  optimize();
  int T;
  cin >> T;
  while (T--)
    solve();
}
